[project]
name = "anime-character-selector"
version = "1.58.0"
description = "面向 WAI Illustrious 的二次元角色选择器，提供中文角色名和英文模型标签下拉列表。"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = []
classifiers = [
    "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/R0smontis/comfyui_anime_character_selector"
"Bug Tracker" = "https://github.com/R0smontis/comfyui_anime_character_selector/issues"

[tool.comfy]
PublisherId = "r0smontis"
DisplayName = "二次元角色选择器 / Anime Character Selector"
Icon = ""
web = "web"
